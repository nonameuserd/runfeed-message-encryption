{
  "name": "@runfeed/message-encryption",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint . --max-warnings 0",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@agentversectl/config-eslint": "workspace:*",
    "@agentversectl/config-typescript": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}
